#!/bin/sh
#
# Check that the transfer rate counter changes.
#
# $Id$

(dd if=/dev/zero bs=100 count=1 2>/dev/null;
 sleep 1;
 dd if=/dev/zero bs=100 count=1 2>/dev/null;
) | $PROG -f -r >/dev/null 2>$TMP1
NUM=`tr '\r' '\n' < $TMP1 | sort | uniq -u | wc -l | tr -d ' '`
test $NUM -gt 2

# EOF $Id$
